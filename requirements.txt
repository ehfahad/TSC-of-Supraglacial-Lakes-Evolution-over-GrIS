# Core RPS-GMM pipeline (src/rpsgmm, scripts/run_rps_gmm.py).
#
# numpy, scipy and scikit-learn are pinned: GaussianMixture depends on k-means
# initialization, whose behavior has changed across scikit-learn releases.
# Pinning keeps runs comparable with each other and with the checked-in figures.
numpy==1.26.4
scipy==1.13.0
scikit-learn==1.4.2
pandas>=2.0,<3.0
joblib>=1.3
matplotlib>=3.7
seaborn>=0.13

# Preprocessing from the raw Zenodo archive (notebooks/01_preprocessing,
# scripts/build_processed_data.py). Not needed to run the experiments.
xarray>=2023.6
netCDF4>=1.6
geopandas>=0.13
requests>=2.31

# Notebook execution.
jupyter>=1.0
ipykernel>=6.25

# Baselines only (notebooks/03_baselines, scripts/run_baselines.py).
# Install these in a SEPARATE environment -- sktime pins an older scikit-learn
# than the core pipeline above. See the README, "Reproducing the results".
#   pip install "sktime==0.30.1" "tensorflow>=2.13,<2.16" "dtaidistance>=2.3"
