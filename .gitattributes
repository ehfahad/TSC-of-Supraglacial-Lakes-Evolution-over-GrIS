# Normalize line endings so the repository is identical on Windows, macOS and Linux.
* text=auto eol=lf

# Binary assets: never touch these.
*.png   binary
*.pdf   binary
*.nc    binary
*.zip   binary
